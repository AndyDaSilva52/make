{
    "url": "/api/orders/feed",
    "method": "GET",
    "qs": {
        "maxlot": "{{parameters.maxlot}}"
    },
    "response": {
        "output": "{{item}}",
        "iterate": "{{body}}"
    }
}