{
    "response": {
        "limit": 100,
        "output": {
            "date": "{{parseDate(item.date_created, 'YYYY-MM-DD hh:mm:ss', 'utc')}}",
            "label": "{{item.id}}"
        }
    }
}